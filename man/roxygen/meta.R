list(
  rd_family_title = list(
    components = "components object",
    components_reference = "reference objects",
    components_schema = "schema objects",
    components_security_schemes = "security scheme objects",
    info = "info object",
    paths = "paths object",
    security = "security object",
    servers = "servers object",
    rapid = "rapid object"
  )
)
