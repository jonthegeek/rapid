# Can create rapid objects

    Code
      test_result <- rapid(info = given_info)
      test_result
    Output
      <rapid[1]>
            info 
      Sample API 

---

    Code
      rapid()
    Condition
      Error in `rapid()`:
      ! argument "info" is missing, with no default

